# Computer_Graphics_Project
Blender add-on that uses a local MLLM (via Ollama) to automatically generate 3D scene layouts from text descriptions. Objects are placed and oriented in the scene through an AI-generated JSON structure, applied via Python scripting.
